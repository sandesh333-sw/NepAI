apiVersion: v1
kind: Service
metadata:
  name: nepai-service
  namespace: nepai
  labels:
    app: nepai
spec:
  type: ClusterIP  # Internal only, Traefik will expose it
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: nepai